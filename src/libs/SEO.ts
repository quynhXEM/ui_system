export const SEO = async (lang: string) => {
  // const myHeaders = new Headers()

  // myHeaders.append('Authorization', `Bearer ${process.env.NEXT_PUBLIC_DEV_STATIC_TOKEN}`)

  // const requestOptions: RequestInit = {
  //   method: 'GET',
  //   headers: myHeaders,
  //   redirect: 'follow'
  // }

  // const metadata = await fetch(
  //   `${process.env.NEXT_PUBLIC_API_URL}/items/websites/${process.env.NEXT_PUBLIC_WEBSITE_ID}?fields=*,translations.*&deep[translations][_filter][language_code]=${lang}`,
  //   requestOptions
  // )
  //   .then(response => response.json())
  //   .then(result => result.data)
  //   .catch(error => console.error(error))

  return null
}
